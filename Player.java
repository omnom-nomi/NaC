public class Player {
    private Board gameBoard;

    public Player(Board board) {
        this.gameBoard = board;
    }

    public boolean makeMove(int x, int y) {
        // TODO: Make the player's move and return success status
        return false;
    }
}
